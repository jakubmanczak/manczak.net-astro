<div class="buttonsContainer">
	<a href="/info" class="card">
		<img src="/assets/user.svg" alt="user icon">
		About Me
	</a>
	<a href="/read" class="card">
		<img src="/assets/edit.svg" alt="pencil icon">
		Writings
	</a>
	<a href="/dash" class="card">
		<img src="/assets/clipboard.svg" alt="clipboard icon">
		Projects
	</a>
</div>

<style lang="scss" scoped>
	.buttonsContainer{
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		align-items: center;
		gap: 1rem;
		.card{
			display: flex;
			align-items: center;
			gap: .7rem;
			font-family: var(--font-big);
			background: var(--dark1);
			animation-name: buttonAnim;
			img{
				filter: invert(55%) sepia(42%) saturate(726%)
				hue-rotate(216deg) brightness(84%) contrast(86%);
				// corresponds to var(--purple)
			}
			&:hover{
				background: var(--dark2);
				color: var(--lite1);
				img{
					filter: invert(70%) brightness(125%);
				}
			}
			&:focus{
				border: 2px solid var(--purp1);
				color: var(--lite1);
				img{
					filter: invert(70%) brightness(125%);
				}
			}
			&:nth-of-type(1){ animation-duration: .75s; }
			&:nth-of-type(2){ animation-duration: 1s; }
			&:nth-of-type(3){ animation-duration: 1.25s; }
		}
	}
	@media screen and (max-width: 800px){
		.buttonsContainer{
			flex-direction: column;
			gap: 0;
			.card{
				margin: .5rem 0;
				width: 50%;
				justify-content: center;
			}
		}
	}
	@keyframes buttonAnim{
		0%{ opacity: 0; transform: scale(.9) translateY(1rem); }
		25%{ opacity: 0; transform: scale(.9) translateY(1rem); }
		100%{ opacity: 1; transform: scale(1) translateY(0); }
	}
</style>