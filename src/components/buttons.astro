<div class="buttonsContainer">
	<a href="/info" class="card">
		<img src="/assets/svg/user.svg" alt="user icon">
		About Me
	</a>
	<!-- <a href="/read" class="card">
		<img src="/assets/svg/edit.svg" alt="pencil icon">
		Writings
	</a> -->
	<a href="/urls" class="card">
		<img src="/assets/svg/edit.svg" alt="pencil icon">
		Links
	</a>
	<a href="/dash" class="card">
		<img src="/assets/svg/clipboard.svg" alt="clipboard icon">
		Projects
	</a>
</div>

<style lang="scss" scoped>
	.buttonsContainer{
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		align-items: center;
		gap: 1rem;
		.card{
			display: flex;
			align-items: center;
			gap: .7rem;
			font-family: var(--font-big);
			background: var(--dark1);
			animation-name: buttonAnim;
			img{
				filter: invert(55%) sepia(42%) saturate(726%)
				hue-rotate(216deg) brightness(84%) contrast(86%);
				// corresponds to var(--purple)
			}
			&:hover{
				background: var(--dark2);
				color: var(--lite1);
				img{
					filter: invert(70%) brightness(125%);
				}
			}
			&:focus{
				border: 2px solid var(--purp1);
				color: var(--lite1);
				img{
					filter: invert(70%) brightness(125%);
				}
			}
			&:nth-of-type(1){ animation-duration: calc(var(--delayed-animation-duration) + ( 1 * var(--delayed-animation-duration-delay) ) ); }
			&:nth-of-type(2){ animation-duration: calc(var(--delayed-animation-duration) + ( 2 * var(--delayed-animation-duration-delay) ) ); }
			&:nth-of-type(3){ animation-duration: calc(var(--delayed-animation-duration) + ( 3 * var(--delayed-animation-duration-delay) ) ); }
		}
	}
	@media screen and (max-width: 800px){
		.buttonsContainer{
			flex-direction: column;
			gap: 0;
			.card{
				margin: .5rem 0;
				width: 50%;
				justify-content: flex-start;
				gap: 1.5rem;
			}
		}
	}
	@keyframes buttonAnim{
		0%{ opacity: 0; transform: scale(.9) translateY(1rem); }
		25%{ opacity: 0; transform: scale(.9) translateY(1rem); }
		100%{ opacity: 1; transform: scale(1) translateY(0); }
	}
</style>